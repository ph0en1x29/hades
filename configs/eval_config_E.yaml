# Config E: Deferred research configuration
experiment:
  id: config_e
  name: "Deferred: alternative local model baseline"
  description: "Not part of the scoped v1 benchmark contract on main"
  status: deferred_research_only

notes:
  - Keep outside the critical path until the local benchmark harness exists.
