# Config D: Deferred research configuration
experiment:
  id: config_d
  name: "Deferred: native swarm comparison"
  description: "Not part of the scoped v1 benchmark contract on main"
  status: deferred_research_only

notes:
  - Native swarm remains a post-v1 research question.
  - This file is intentionally non-executable until the scope expands.
