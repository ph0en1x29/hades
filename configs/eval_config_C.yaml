# Config C: Deferred research configuration
experiment:
  id: config_c
  name: "Deferred: OpenClaw orchestration comparison"
  description: "Not part of the scoped v1 benchmark contract on main"
  status: deferred_research_only

notes:
  - Retained only to document a possible later comparison.
  - Not supported by the current runtime or v1 evaluation scope.
