# Config H: Deferred research configuration
experiment:
  id: config_h
  name: "Deferred: secondary cloud comparison"
  description: "Not part of the scoped v1 benchmark contract on main"
  status: deferred_research_only

notes:
  - Retained only as a note for later research once offline v1 is complete.
