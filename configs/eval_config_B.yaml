# Config B: Deferred research configuration
experiment:
  id: config_b
  name: "Deferred: local ablation after v1"
  description: "Not part of the scoped v1 benchmark contract on main"
  status: deferred_research_only

notes:
  - Retained as a placeholder for later ablation work.
  - Requires a separate implementation plan and benchmark transformation pipeline.
